#!/bin/sh

set -e

umask 077

basedir=~/.topaz
dbfile="$basedir/DB_CONFIG"
cfgfile="$basedir/topaz.conf"

[ -e "$basedir" ] || mkdir "$basedir"

# Topaz does not clean up DB log files by default
[ -e "$dbfile" ] || echo 'set_flags DB_LOG_AUTOREMOVE' > "$dbfile"

exec /usr/lib/topaz/topaz-qt "$@"
